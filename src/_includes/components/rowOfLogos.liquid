<div class="flex-row">
                {% for logo in content.logos %}
                    {% capture imgUrl %}
                        {% sanityImageUrl logo %}
                    {% endcapture %}
                    {% image imgUrl, '' %}
                {% endfor %}
            </div>