<a class="button {{ buttonType }}" href="{{ buttonLink }}">
    {{ buttonText }}
    <span>
        {% if buttonIcon %}
            {% include 'svg/arrow-right.svg' %}
        {% endif %}
    </span>
</a>