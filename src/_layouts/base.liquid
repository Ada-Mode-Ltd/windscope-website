<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="/assets/css/index.css">
  </head>
  <body class="wrapper">
    {% unless navComponent %}
    {% include compnents/pageNav.liquid %}
    {% else %}
    {% include navComponent %}
    {% endunless %}
    {{ content }}
  </body>
</html>